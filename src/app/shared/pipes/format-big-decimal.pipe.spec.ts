import { FormatBigDecimalPipe } from './format-big-decimal.pipe';

describe('FormatBigDecimalPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatBigDecimalPipe();
    expect(pipe).toBeTruthy();
  });
});
